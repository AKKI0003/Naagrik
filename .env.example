# Leave unset to run entirely in-browser against LocalReportsRepository
# (localStorage) — zero setup, single-device demo, good for early dev.
#
# Set this once /server is deployed to switch to the real multi-user
# Node/Postgres+PostGIS backend from the architecture slide:
# VITE_API_BASE_URL=https://your-nagrik-api.onrender.com
